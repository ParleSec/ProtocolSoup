apis:
  gateway@v1:
    root: openapi/gateway.yaml
  scim@v1:
    root: openapi/scim.yaml

extends:
  - recommended

rules:
  info-license: off
  operation-operationId-unique: error
  no-unused-components: warn
