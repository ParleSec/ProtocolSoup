openapi: 3.1.0
info:
  title: ProtocolSoup SCIM API
  version: 1.0.0
  summary: SCIM 2.0 user and group provisioning endpoints.
  description: |
    Public contract for the ProtocolSoup SCIM service.
    Implements RFC 7642/7643/7644 style resource provisioning and discovery.
servers:
  - url: https://protocolsoup.com
    description: Production SCIM service
security: []
tags:
  - name: Health
    description: Service health and API index endpoints.
  - name: Discovery
    description: SCIM discovery resources.
  - name: Users
    description: SCIM user lifecycle operations.
  - name: Groups
    description: SCIM group lifecycle operations.
  - name: Bulk
    description: SCIM bulk and search operations.
  - name: Client
    description: Authenticated outbound SCIM provisioning operations.
paths:
  /health:
    get:
      tags: [Health]
      summary: Get service health
      operationId: getScimHealth
      responses:
        "200":
          description: Health payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: Service unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api:
    get:
      tags: [Health]
      summary: Get API index
      operationId: getScimApiIndex
      responses:
        "200":
          description: API index payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiIndexResponse"
        "503":
          description: Service unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /scim/v2/ServiceProviderConfig:
    get:
      tags: [Discovery]
      summary: Get service provider capabilities
      operationId: getServiceProviderConfig
      security:
        - bearerAuth: []
      responses:
        "200":
          description: SCIM service provider configuration.
          content:
            application/scim+json:
              schema:
                type: object
                additionalProperties: true
            application/json:
              schema:
                type: object
                additionalProperties: true
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/ResourceTypes:
    get:
      tags: [Discovery]
      summary: List resource types
      operationId: listResourceTypes
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Resource type list.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/ResourceTypes/{id}:
    get:
      tags: [Discovery]
      summary: Get resource type by ID
      operationId: getResourceType
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Resource type descriptor.
          content:
            application/scim+json:
              schema:
                type: object
                additionalProperties: true
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Resource type not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Schemas:
    get:
      tags: [Discovery]
      summary: List SCIM schemas
      operationId: listSchemas
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Schema list.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
            application/json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Schemas/{id}:
    get:
      tags: [Discovery]
      summary: Get schema by ID
      operationId: getSchema
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Schema descriptor.
          content:
            application/scim+json:
              schema:
                type: object
                additionalProperties: true
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Schema not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Users:
    get:
      tags: [Users]
      summary: List or filter users
      operationId: listUsers
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: filter
          schema:
            type: string
          description: SCIM filter expression.
        - in: query
          name: startIndex
          schema:
            type: integer
            minimum: 1
        - in: query
          name: count
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: User list response.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
              examples:
                users:
                  value:
                    schemas:
                      - urn:ietf:params:scim:api:messages:2.0:ListResponse
                    totalResults: 1
                    startIndex: 1
                    itemsPerPage: 1
                    Resources:
                      - id: "2819c223-7f76-453a-919d-413861904646"
                        userName: "alice@example.com"
                        active: true
        "400":
          description: Invalid filter or request parameters.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    post:
      tags: [Users]
      summary: Create user
      operationId: createUser
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/ScimUser"
            examples:
              create:
                value:
                  schemas:
                    - urn:ietf:params:scim:schemas:core:2.0:User
                  userName: "newuser@example.com"
                  name:
                    givenName: "New"
                    familyName: "User"
                  emails:
                    - value: "newuser@example.com"
                      primary: true
          application/json:
            schema:
              $ref: "#/components/schemas/ScimUser"
      responses:
        "201":
          description: User created.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimUser"
        "400":
          description: Invalid user payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Users/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    get:
      tags: [Users]
      summary: Get user by ID
      operationId: getUser
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User resource.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimUser"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: User not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    put:
      tags: [Users]
      summary: Replace user
      operationId: replaceUser
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/ScimUser"
      responses:
        "200":
          description: Replaced user.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimUser"
        "400":
          description: Invalid request payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: User not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    patch:
      tags: [Users]
      summary: Patch user attributes
      operationId: patchUser
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/PatchOpRequest"
      responses:
        "200":
          description: Updated user.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimUser"
        "400":
          description: Invalid patch request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: User not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    delete:
      tags: [Users]
      summary: Delete user
      operationId: deleteUser
      security:
        - bearerAuth: []
      responses:
        "204":
          description: User deleted.
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: User not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Groups:
    get:
      tags: [Groups]
      summary: List groups
      operationId: listGroups
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Group list response.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    post:
      tags: [Groups]
      summary: Create group
      operationId: createGroup
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/ScimGroup"
      responses:
        "201":
          description: Group created.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimGroup"
        "400":
          description: Invalid group payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Groups/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
    get:
      tags: [Groups]
      summary: Get group by ID
      operationId: getGroup
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Group resource.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimGroup"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Group not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    put:
      tags: [Groups]
      summary: Replace group
      operationId: replaceGroup
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/ScimGroup"
      responses:
        "200":
          description: Replaced group.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimGroup"
        "400":
          description: Invalid group payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Group not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    patch:
      tags: [Groups]
      summary: Patch group
      operationId: patchGroup
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/PatchOpRequest"
      responses:
        "200":
          description: Updated group.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimGroup"
        "400":
          description: Invalid patch payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Group not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
    delete:
      tags: [Groups]
      summary: Delete group
      operationId: deleteGroup
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Group deleted.
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "404":
          description: Group not found.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/Bulk:
    post:
      tags: [Bulk]
      summary: Execute bulk operations
      operationId: bulkOperations
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              $ref: "#/components/schemas/BulkRequest"
      responses:
        "200":
          description: Bulk response payload.
          content:
            application/scim+json:
              schema:
                type: object
                additionalProperties: true
        "400":
          description: Invalid bulk payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/v2/.search:
    post:
      tags: [Bulk]
      summary: Execute POST-based search
      operationId: searchScimResources
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/scim+json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Search result list.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimListResponse"
        "400":
          description: Invalid search payload.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Unauthorized request.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/client/provision:
    post:
      tags: [Client]
      summary: Provision a single user to an external SCIM service
      description: |
        Sends one CreateUser request to the target's exact SCIM base URL. Server-managed
        attributes (id, meta, groups, password) are stripped before the request is sent;
        the source id is preserved as externalId for correlation. This endpoint does not
        persist a source-to-target mapping; use /scim/client/sync for repeatable
        reconciliation across many users.
      operationId: provisionUserToExternalScimService
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientProvisionRequest"
      responses:
        "200":
          description: The user was created on the target SCIM service.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ScimUser"
        "400":
          description: Invalid request, missing userName, or invalid target URL.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Missing or invalid service bearer token.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "502":
          description: The target SCIM service rejected the request or returned an invalid response.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
  /scim/client/sync:
    post:
      tags: [Client]
      summary: Reconcile local users to an external SCIM service
      description: |
        Discovers the target ServiceProviderConfig and performs a one-way reconciliation.
        New users are created and previously mapped users are replaced with PUT. Successful
        source-to-target ID mappings are persisted for later runs. Remote users are never
        deleted implicitly; deprovisioning requires an explicit policy.
      operationId: syncUsersToExternalScimService
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClientSyncRequest"
      responses:
        "200":
          description: Every source user was reconciled successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientSyncResponse"
        "207":
          description: Reconciliation completed with one or more per-user failures.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClientSyncResponse"
        "400":
          description: Invalid request or target URL.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "401":
          description: Missing or invalid service bearer token.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
        "502":
          description: Target discovery or SCIM operations could not be completed.
          content:
            application/scim+json:
              schema:
                $ref: "#/components/schemas/ScimError"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: token
  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
    HealthResponse:
      type: object
      required: [status, version, protocols, plugins]
      properties:
        status:
          type: string
          example: healthy
        version:
          type: string
          example: 1.0.0
        protocols:
          type: array
          items:
            type: string
          example: [scim]
        plugins:
          type: array
          items:
            $ref: "#/components/schemas/PluginHealth"
    PluginHealth:
      type: object
      required: [plugin_id, state, healthy]
      properties:
        plugin_id:
          type: string
        state:
          type: string
          enum: [uninitialized, initializing, ready, shutting_down, stopped, error]
        healthy:
          type: boolean
        error:
          type: string
    ClientProvisionRequest:
      type: object
      required: [targetUrl, user]
      properties:
        targetUrl:
          type: string
          format: uri
          description: Exact base URL of the target SCIM service, such as https://app.example.com/scim/v2.
        authToken:
          type: string
          writeOnly: true
          description: Bearer token sent only to the target SCIM service.
        user:
          $ref: "#/components/schemas/ScimUser"
    ClientSyncRequest:
      type: object
      required: [targetUrl]
      properties:
        targetUrl:
          type: string
          format: uri
          description: Exact base URL of the target SCIM service, such as https://app.example.com/scim/v2.
        authToken:
          type: string
          writeOnly: true
          description: Bearer token sent only to the target SCIM service.
    ClientSyncResponse:
      type: object
      required: [status, targetUrl, startedAt, completedAt, sourceUserCount, result]
      properties:
        status:
          type: string
          enum: [completed, partial]
        targetUrl:
          type: string
          format: uri
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
        sourceUserCount:
          type: integer
          minimum: 0
        lastSuccessfulSync:
          type: string
          format: date-time
        result:
          $ref: "#/components/schemas/ClientSyncResult"
    ClientSyncResult:
      type: object
      required: [created, updated, deleted, errors, mappings]
      properties:
        created:
          type: integer
          minimum: 0
        updated:
          type: integer
          minimum: 0
        deleted:
          type: integer
          minimum: 0
          description: Always zero because this endpoint does not infer deprovisioning.
        errors:
          type: integer
          minimum: 0
        mappings:
          type: object
          additionalProperties:
            type: string
        details:
          type: array
          items:
            $ref: "#/components/schemas/ClientSyncOperation"
    ClientSyncOperation:
      type: object
      required: [localId, operation, status]
      properties:
        localId:
          type: string
        remoteId:
          type: string
        operation:
          type: string
          enum: [create, update]
        status:
          type: string
          enum: [success, error]
        error:
          type: string
    ApiIndexResponse:
      type: object
      required: [service, version, protocols, endpoints]
      properties:
        service:
          type: string
        version:
          type: string
        protocols:
          type: array
          items:
            type: string
        endpoints:
          type: object
          additionalProperties:
            type: string
    ScimListResponse:
      type: object
      required: [schemas, totalResults, Resources]
      properties:
        schemas:
          type: array
          items:
            type: string
          example:
            - urn:ietf:params:scim:api:messages:2.0:ListResponse
        totalResults:
          type: integer
          minimum: 0
        startIndex:
          type: integer
          minimum: 1
        itemsPerPage:
          type: integer
          minimum: 0
        Resources:
          type: array
          items:
            type: object
            additionalProperties: true
    ScimUser:
      type: object
      required: [schemas]
      properties:
        schemas:
          type: array
          items:
            type: string
        id:
          type: string
        userName:
          type: string
        active:
          type: boolean
        name:
          type: object
          properties:
            givenName:
              type: string
            familyName:
              type: string
        emails:
          type: array
          items:
            type: object
            properties:
              value:
                type: string
              type:
                type: string
              primary:
                type: boolean
        meta:
          type: object
          additionalProperties: true
    ScimGroup:
      type: object
      required: [schemas]
      properties:
        schemas:
          type: array
          items:
            type: string
        id:
          type: string
        displayName:
          type: string
        members:
          type: array
          items:
            type: object
            properties:
              value:
                type: string
                description: Member resource ID
              display:
                type: string
                description: Member display name
              uri:
                type: string
                description: URI reference to the member resource (SCIM `$ref` field)
        meta:
          type: object
          additionalProperties: true
    PatchOpRequest:
      type: object
      required: [schemas, Operations]
      properties:
        schemas:
          type: array
          items:
            type: string
          example:
            - urn:ietf:params:scim:api:messages:2.0:PatchOp
        Operations:
          type: array
          items:
            type: object
            required: [op]
            properties:
              op:
                type: string
                enum: [add, replace, remove]
              path:
                type: string
              value:
                oneOf:
                  - type: object
                    additionalProperties: true
                  - type: array
                    items:
                      type: object
                      additionalProperties: true
                  - type: string
                  - type: number
                  - type: boolean
                  - type: "null"
    BulkRequest:
      type: object
      required: [schemas, Operations]
      properties:
        schemas:
          type: array
          items:
            type: string
          example:
            - urn:ietf:params:scim:api:messages:2.0:BulkRequest
        failOnErrors:
          type: integer
          minimum: 0
        Operations:
          type: array
          items:
            type: object
            required: [method, path]
            properties:
              method:
                type: string
                enum: [POST, PUT, PATCH, DELETE]
              path:
                type: string
              bulkId:
                type: string
              data:
                type: object
                additionalProperties: true
    ScimError:
      type: object
      required: [schemas, detail, status]
      properties:
        schemas:
          type: array
          items:
            type: string
          example:
            - urn:ietf:params:scim:api:messages:2.0:Error
        detail:
          type: string
        status:
          type: string
        scimType:
          type: string
